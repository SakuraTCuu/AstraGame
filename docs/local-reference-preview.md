# Local Reference Preview

The user authorized temporary use of their local WeChat resource cache on 2026-09-05. Cached content, generated profiles, tables, and screenshots remain in ignored local directories. They are not part of the Git repository or the Zhushen module export.

## Run

Build the project with Cocos Creator 2.4.15, then stage the authorized cache:

```powershell
node tools/stage-reference-cache.mjs --cache '<authorized gamecaches directory>'
```

Serve `build/web-mobile`, and open `http://127.0.0.1:4174/?reference=1`. The reference mode is restricted to a localhost browser URL. The default URL continues to use the independent combat fixture. Restage after a Creator build or after visiting additional areas in the reference game.

The optional `tools/fetch-reference-map.mjs` command accepts `--cache`, `--base-url`, and `--map`. It compares the remote map configuration with the supplied cache before downloading missing PNG tiles. Downloads and their SHA256 manifest remain in ignored `reference-private/downloads`; staging includes this supplement automatically. It never changes the WeChat cache.

## Asset Pipeline

- `cacheList.json` resolves original import/native paths to cached files.
- The base and extension bundle configurations are joined by decoded UUID. Pack ordering and version indices are preserved, including textures referenced across the two parts.
- Resource-only bundles load through Cocos `assetManager`. Original game scripts are not loaded.
- Table archives use bounded length fields, the compression flag, zlib, and JSON. `__KEY_MAP__` supplies field names; `$id` references resolve within the same field.
- `profile.json` supplies the local scene and presentation bindings. `audit.json` records missing art and adaptation limits.
- Ground textures use their logical slice dimensions. Native cache images may be reduced to 819 pixels while representing 1024 logical pixels.
- Visible map tiles load on demand and release when outside the viewport. Character nodes follow the existing combat snapshots.

## Current Boundary

The preview loads the actual first world map, an initial party of four Spine characters, 135 enemy/resource templates and 2489 configured spawn points. Missing art no longer removes an encounter from the simulation. The original collision grid is sampled into a 40-unit navigation grid. All 224 source route points map to unblocked source cells; this validates the coordinate conversion, not every sampled boundary or gameplay restriction.

The current first-map supplement fills 648 missing tiles from the verified resource base. All 844 required ground tiles are present, with zero missing paths. There are 845 texture entries including the thumbnail, which remains useful for the overview; detailed ground is loaded by viewport.

The reference adapter includes 59 fog gates, 31 portals and 73 additional source interactions, including chests. Source item costs, level/rank/defeat/quest/NPC conditions and incense gathering probabilities are retained. Chests follow the source `autoOpen` flag. Fog rectangles are converted from the editor's 120-unit grid into convex world polygons. The overview supports panning, zooming, destination selection and repaired-portal travel. Opening it pauses the standalone simulation; this behavior still needs reference comparison.

Exploration saves are isolated by configuration and, in Zhushen, by role. They preserve balances, level/experience, counted kills, flags, opened gates, explored cells, party positions/HP/energy, rolled equipment and slot assignments, hero levels, the random state, permanent clears and respawn timers. Existing saves without the newer fields retain their configured defaults. Explicit restart clears this exploration save. Active buffs, in-progress enemy HP and cast/cooldown state are not yet resumed.

The standalone comparison preset starts at world level 1 and rank 1 with 20 incense, its home portal repaired and fog containing the spawn open. It begins the first-world mainline after the tutorial's return-to-town step. The four heroes use level-10 attributes from the source tables. These are explicit comparison-preset choices, not the user's live account.

Source item 7 rewards now advance team experience using all 1525 `PlayerLevel` rows. The first world has 98 enemy templates with experience rewards. Experience carries across thresholds and immediately refreshes level-gated exploration. Kill-count conditions retain their required counts; one historical defeat is not treated as completion of a multi-kill task. Daily gain limits and account-dependent reward scaling remain unconfirmed. At the last configured level the local simulation stops accumulating experience.

The journal contains 156 mainline steps, 30 first-Boss-reward entries and 324 rank tasks across 109 configured ranks. It enforces prerequisites, counted kills, monster subtype counts, cumulative item gains, party-level checks and claim-once flags. Inventory rewards and claims persist across reloads, including compatibility with saves that predate the added currencies. The task tracker and journal expose navigation, reward claims and promotion. Task routes approach the first blocking fog entrance; paying the entrance resumes the original destination without bypassing the lock.

The first source promotion and nine mainline steps are playable through the first Boss, equipment assignment and the first seal milestone. Eight source condition rows still require other game modes. The recruitment and deployed-hero counters now follow their corresponding commands; rename still needs its standalone account action. Configuration conversion alone does not make those later tasks playable. The journal starts from an explicitly selected post-tutorial chapter rather than marking the complete campaign as reproduced.

Equipment is assigned per party position, using 48 source slots and their unlock conditions. Swapping heroes transfers the position's equipment bonuses without duplicating the item. The staged inventory currently includes ten equipment definitions. New copies receive persistent, seeded attribute rolls; a single item cannot occupy two slots. Supported first-defeat rewards have their own persistent claim flags. All supported ordinary item drops now reach inventory, including vouchers and merit. Twenty-three later first-defeat reward references use an unimplemented `RepeatRand` reward table and remain recorded in `audit.json`.

Hero growth uses shared, indexed level tables for source types 31, 41, 51, 61, 71 and 81, each containing 22,550 rows. Upgrades spend the current row's configured cost and enforce the current rank's hero-level limit. Source rank attack, defense and max-health bonuses are applied. Changing equipment or level preserves the actor's health ratio and does not revive dead characters. The comparison party remains level 10 even though its initial rank has a lower cap; it cannot upgrade further until the configured cap permits it. Upgrade cost-row timing and health-ratio behavior still need direct live comparison, and star/ascension or special equipment attributes are not covered by this implementation.

Energy capacity and energy gains now follow the configured hero-level thresholds. The source profiles first enable their ultimate-energy fields at level 10. Upgrading a newly owned hero updates those fields as well as attack, defense and health; restoring its level precedes saved-energy validation. Only changed energy tiers are serialized, retaining the shared level-table layout.

The roster uses eight source positions, with the fifth through eighth unlocked at ranks 4, 10, 19 and 28. It includes 44 profiles, initially showing 33, while 23 have deployable combat/art adapters. Unsupported heroes retain an unavailable state. Owned heroes can enter, leave and exchange positions. Reserve health and energy persist; benching cancels active casts while retaining existing cooldown deadlines. Save restoration validates ownership, slot gates and deployment availability, and migrates earlier four-hero saves. The observed account's owned collection is not copied into the comparison preset.

Ordinary recruitment uses source voucher costs, reward entries, group weights and the guarantee curve. Draws persist the lifetime count and guarantee progress, and reward cards unlock roster ownership. Fragments and duplicate cards remain inventory items. Fragment activation, duplicate conversion, star growth, wishlists, daily guarantees and other pools are not implemented. Prize-first selection and the interpretation of unlock-dependent group weights require further reference comparison; table conversion is not proof of exact combined probabilities.

The source main map enables revival, and its localization includes town and nearest-portal return commands. The local profile now enters a recoverable state after a full wipe. Returning restores the party with its existing equipment and levels, keeps exploration and resources, clears old casts/projectiles/cooldowns and resets surviving enemies to their home positions and initial Boss phases. It chooses the nearest repaired, unlocked portal by world distance. The death position survives reload, and this state does not submit a terminal run result. These mechanics remain an implementation to compare against the original: fees, timers, in-place revival, partial-party recovery, energy reset and exact enemy-reset/portal-selection behavior have not been directly observed in the live game. The independent fixture retains terminal failure when recovery is not configured.

The skill adapter compiles source coefficients, pre/post timing, target counts, projectiles, public cooldown groups, energy costs, HP/time gates and supported buffs. Comparison heroes start at zero stars; skills with later star requirements are excluded, including otherwise hidden passive groups. Alternate-form suffixes remain audited and higher-star skill changes are not exposed as an upgrade command yet. The core resolves multi-hit timelines, homing projectiles, healing, forced critical hits, attack-speed modifiers, charges and jumps. Boss HP phases come from their skill conditions; timed enrage is a separate status.

Periodic attack-based damage now uses the source intervals, coefficients, stack limits and interval-per-stack tags. Statuses retain their applying hero for damage and kill credit, include a tick at the expiry boundary, refresh duration on reapplication and share the ordinary shield/death/reward pipeline. The adapter distinguishes soul, magic and physical source damage IDs; physical and magic bonuses no longer apply to soul damage. Periodic bonuses/reductions are separate. The source hero-26 ultimate can settle a configured duration of its target's existing bleed.

Periodic parity still needs direct live checks: damage uses current caster attributes, all layers share an expiry/tick clock, and immediate settlement adds full ticks without consuming the status. Fractional settlement, snapshot versus current attributes, critical behavior and conditional passives remain unconfirmed or incomplete. The existing defense formula is still provisional. Supporting these periodic actions does not mean every skill of the newly deployable heroes is complete.

Unconditional permanent character traits now become the owning actor's intrinsic modifiers. They are not copied to teammates. This includes supported soul/physical/periodic bonuses, health percentage and PvE-only reduction; conditional traits and unsupported chance-based effects stay in the audit. The tank passive's redundant trailing bracket is normalized with an explicit audit entry. Soul vulnerability retains its negative reduction sign. Holy, punishment and untyped skill damage retain separate identities instead of inheriting physical modifiers; their specialized formulas remain incomplete.

Intrinsic and temporary health modifiers preserve health ratio when their cap changes. Saves normalize temporary health bonuses out of active and reserve health, since temporary statuses are not resumed, while retaining intrinsic traits. Health scaling currently uses the computed base cap, including equipment/rank contributions; that scope and rounding still need live comparison.

Normal-attack and tactical-skill energy gains are now separate. The normal baseline is 2% of capacity, confirmed in two live character panels and consistent with source scripted-character defaults; explicit table values take precedence. Tactical percentages come from the level table. A cast awards energy only on its first resolving effect, sharing that award across its hits and projectiles. Fully missed/cancelled actions, periodic ticks, blocked procs and ultimates do not generate extra cast energy. A benched projectile owner cannot gain reserve energy. Energy-gain efficiency affects positive gains, not spending. Passive regeneration remains mapped from `ultraEnegyRecoverRate`; its live timing and special energy tags still need comparison, as does the exact original hit-versus-cast boundary.

Healing bonuses distinguish numeric power increases from weighted or all-status awards, with caster-status presence/absence conditions. Timed target-count bonuses apply to the configured skill group. Additional release-triggered skills use their own target selection/timeline without taking over the primary cast; cancellation, death and encounter resets clear pending extra casts. Cyclic links are bounded. The source adapter currently accepts unconditional, motionless, zero-cost additional skills; other trigger conditions remain audited.

Source hero 16's three-second healing state, immediate three-target extra heal, enhanced normal heal and NPC-debuff cleanse are connected. Cleansing preserves player-origin effects (including captured summon provenance), protected effects and beneficial effects. Harmful classification currently covers adapted negative modifiers, periodic damage and recognized control states. Unknown state classifications, the exact live trigger boundary and higher-star mechanics still need comparison. These checks do not establish complete character parity.

Supported impact knockback and damage-based self healing attach to their damage frame and share its target selection. Knockback uses collision-aware displacement, interrupts pending casts, blocks voluntary movement during displacement and replans the leader's route afterward. Explicit displacement immunity and actor removal/death/reset cleanup are supported. The renderer selects a hurt animation when present, falling back to idle without continuing an interrupted attack. Source hero 10's tactical and ultimate now use these effects; hero 5's supported projectile damage also carries its knockback.

The adapter currently interprets `repelAction` as duration in milliseconds followed by distance, with linear interpolation. Exact units, easing, immunity and interruption rules remain in `knockback_parity` audit entries. The live hero-10 panel confirms the target counts, soul-damage coefficient and 50% self recovery, but does not establish motion timing. Self recovery currently uses actual HP removed after shields and overkill capping. Its source multi-execution passive remains unimplemented, so this does not establish complete ultimate parity.

Source Buffs now preserve every `addStateAction` and its independent duration, including states that outlive their owning Buff. Supported stun/freeze/airborne, root and silence controls constrain their corresponding movement and cast operations. Control immunity, interruption immunity, `noStateTag`, Boss exclusions and `notControlCond` are separate contracts. A protected cast can finish during control or knockback, while additional voluntary casts remain blocked. Freeze pauses the reference animation. Cleansing, death and recovery also handle remaining states after a Buff expires.

Independent timers correct source hero 37's two-second airborne state despite its one-second Buff duration, matching the source skill description. This does not complete that hero's transformation, health costs or alternate attacks. Airborne height and fear movement are now connected to the simulation and renderer. Other specialized state behavior remains recorded as `state_behavior` or `state_options` issues. Existing-control behavior when immunity is gained and precise live interruption boundaries still require comparison.

Airborne motion separates body elevation from the ground point, supports rise/hold/fall phases and preserves current height on reapplication. Both reference art and the independent renderer raise the body and health bar. Fear follows seeded random headings and uses collision on every segment, yielding to other movement-blocking controls. Automatic routes replan after fear ends or is cleansed, including cleanup between world ticks.

Motion parameters remain comparison work: optional fear operands are currently mapped to speed and turn interval in milliseconds; the cached skill description supplies a 350-units/second random-movement baseline with a one-second loop. Explicit `upUp` phases/height are retained. Other airborne actions use the existing local 160-unit jump-height default and a symmetric curve. Default height, easing, the fear interval operand and remaining directional-flight options remain in `airborne_motion_parity` and `fear_motion_parity`; these are not claims of measured live trajectories.

Directional projectile tags now produce moving shots with swept collision, a total hit budget and optional per-target repeat intervals. They no longer resolve their projectile actions as immediate area attacks. The source tornado emits one shot, moves for three seconds and can hit a target again after one second; the source soul projectile stops after three contacts. Target and caster death do not erase an already released shot. Pending contact actions are cleared on reset or target removal.

The adapter currently takes a circular contact radius from the source circle or half the source box width, retaining the full box for warnings. Hitbox interpretation, terrain interaction, multi-shot tags, offsets, special check modes, windup target replacement and art-specific orientation remain unverified. Twelve cached `UiEffect` atlases are bound to projectile snapshots with source frame rates, scale and loop flags. Straight-shot art follows the projected flight direction with horizontal mirroring; homing art retains horizontal mirroring. Directional art variants, missing art and additional effect layers remain in the local audit.

Defensive state markers now affect damage, healing and targeting: `invincible` prevents damage before shields, `notDead` preserves one HP, `unselected` excludes non-self selection and new area/projectile contacts, `unHeal` prevents healing, and `fixOneDmg` caps each hit at one damage. Attached periodic damage retains its own lifecycle but obeys damage protections. Explicit caster-directed state removal supports multiple state names without removing unrelated Buff modifiers.

The current first-map armor Buffs now enforce their three- and six-second one-damage states independently of their enclosing Buffs. Other actions in these armor skills, including conditional damage reduction and break-defense mechanics, remain incomplete. Friendly/global targeting exceptions, exceptional damage types and precise live protection timing still need comparison. Immunity feedback uses the existing combat text pool and is positioned above the reference actor's resource height, including current elevation.

Compound cast costs now preserve every supported resource pair. Ultimate energy, health expenditure and the independent `enegy` counter are checked/debited as one cast transaction; invalid targets or insufficient energy/charges cannot partially spend resources. Charged skills respect their source counter conditions and costs. Direct charge actions and immediate/periodic charge Buffs provide the corresponding gains. Unsupported resource names disable the affected skill and remain audited; free additional skills cannot bypass a child's resource cost.

Health cost currently uses a configured maximum/current-health basis, rounds down and preserves one HP. Source `hp` costs are mapped to maximum health provisionally; the source description confirms the percentage but not this basis or the low-health behavior. Direct `addSkillEnegyAction` operands are currently interpreted as an inclusive random range, while `buffAddEnegyAction` uses a fixed gain and ceiling. These interpretations remain in `health_cost_parity` and `skill_energy_parity` until live comparison. Skill energy persists when an actor is benched but is currently runtime-local, clears on recovery and is not restored with exploration saves.

Supported `sceneSpriteAction` entries now create persistent circular, line or cone areas and execute the separate `sceneSpriteActions` timeline. This connects the source poison pool, following damage areas, a turning channel area, projectile-created areas and the staged healing field. Areas have independent lifetimes and tick clocks, optional per-target limits, PvE/PvP target counts and phased healing effects. Source movement-rate modifiers and target-Buff stack coefficients are supported. Cached area atlases animate on the ground; absent art falls back to the actual area geometry.

The poison source adds its marker before calculating escalating damage, retains that marker for its configured duration after exit, and stops new area damage outside the pool. The healing field applies one heal per selected ally per tick and changes its Buff/heal tier according to the source thresholds. Source aim-lock offsets and forward channel movement are connected. Range-attribute tags, pull variants, additional area visuals, percentage limits and exact live timing/cleanup remain incomplete. Random multi-warning layouts and moving scene-object tags remain audited rather than becoming overlapping static areas at a single location.

The renderer uses the scene's 549 foreground polygons and region light switches. It copies the visible ground into a render texture and composites only foreground pixels over actors. Directional regions use a smaller ambient light plus the flashlight cone.

Still incomplete: defense/critical calibration, specialized enemy actions and conditions, multiple-warning layouts, damage-limit rules, sword-fan timing, full skill effects, advanced recruitment/growth and remaining task actions, random reward groups, complete enemy art and exact overview styling. `audit.json` lists unresolved skill actions and progression conditions instead of claiming complete adaptation.

`node tools/reference-smoke.mjs` validates local assets, movement, a naturally reached battle, atlas animation frames, bounded visible tiles, and three restarts at desktop and mobile sizes. The ordinary `npm run smoke:web -- http://127.0.0.1:4174` continues to validate the independent deterministic combat fixture.

## Validation On 2026-09-05

- 111 deterministic tests passed, including skill-frame parsing and hero star gates, multi-hit cancellation, multi-target homing heals, energy/public cooldown gates, level-dependent energy, periodic expiry/stacking/settlement/kill credit, buff expiry and permanent healing reduction, jump dodging, experience rollover, atomic reward claims, rank prerequisites, fog-gate task routing, equipment damage application, seeded item rolls, upgrade costs, roster/legacy-save validation, recruitment cost/guarantee behavior, natural wipe/recovery, stale projectile cleanup and progression-save compatibility.
- Creator 2.4.15 Web Mobile build succeeded.
- Reference browser smoke opened the overview, selected a gate, navigated to it and clicked its unlock command. The balance changed from 20 to 15. Reload restored the same balance, position and 109 explored cells. Repaired-portal travel changed position without spending more incense. Combat, atlas animation, desktop/mobile views and three restarts passed without console errors or failed requests.
- Desktop 1280 x 800 and mobile 390 x 844 captures were inspected. Detailed map coverage is recorded in `audit.json` as required and missing tile paths.
- Foreground render-texture checks found 3153 covered samples in the starting view. A renderer-only flashlight probe measured alpha 0 ahead versus 64 behind, independently of gameplay progression.
- Three post-combat restarts retained eight world-layer children and four character views. The damage-label pool remained bounded.
- Browser combat earned 20 experience, matching the source rewards for its counted kills. Storage retained the same level, experience and counters; the HUD was inspected at desktop and mobile sizes.
- Browser UI claimed the three initial rank tasks, promoted to rank 2 and claimed the first mainline reward. Mobile and desktop journal captures were inspected. A label-size check prevents Cocos label creation from collapsing the task tracker to zero width.
- Browser navigation reached and defeated the first Boss, acquired its bracer and rendered the source equipment icon. Equipping raised the first hero's attack 92 to 115, defense 23 to 27 and max health 2074 to 2412. Reload preserved those values and the slot assignment. Unequip and transfer to the second hero left exactly one equipped item. Mobile and desktop equipment views were inspected; browser validation also checks the negative unequip preview.
- A separate lethal-damage fixture then exercised the recovery UI. Reload retained the death position, 291 explored cells, 38 incense and the equipped bracer. Both return buttons restored a living party at the selected destination; equipment and resource balances were unchanged, and no previous casts or projectiles remained. Mobile and desktop recovery screenshots were inspected. This is a controlled browser scenario, not an observation of an original-game defeat.
- Browser roster validation exchanged two positions, removed and restored a hero, and recruited twice using earned vouchers. The voucher balance changed from 218 to 18 and the guarantee counter from 60 to 58. An explicit rank/card fixture then deployed a fifth hero and verified persistence through reload. This fixture does not demonstrate naturally reaching rank 4. Portraits and labels were inspected at desktop and mobile sizes.
- The independent fixture still completed its full navigation, unlock and Boss sequence in about 183 seconds of simulated time.
- The source Boss run reached both paid gates and defeated the first Boss in about 89.1 simulated seconds, with three survivors, without teleporting or changing combat stats. It observed normal attacks, charge, jump, phase 2 and death. Natural rewards advanced the team to level 2 with 40/600 experience; reload preserved the level, experience and one Boss kill. Run it with `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-boss-smoke.mjs` after staging.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-journal-smoke.mjs` completed nine mainline steps, a chest, two paid gates, the first Boss and equipment assignment in about 201.1 simulated seconds. Three heroes survived. Claiming the first-Boss reward produced one seal and brought vouchers to 325, including ordinary drops. Reload retained the reward and equipment and refused duplicate claims. It used normal equipment commands and no teleport or stat overrides.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-periodic-smoke.mjs` uses fixture ownership/rank/materials to upgrade source heroes 2 and 26 to level 10, then tests their actual compiled skills against a durable stationary target with prefilled energy. Hero 2 produced three 57-damage ticks one second apart. Hero 26 produced a 49-damage tick, a 99-damage tick after stacking, and two additional 99-damage ticks on its ultimate's impact. This validates the local table-to-runtime path, not original-game damage parity.
- Browser validation separately deployed heroes 2 and 26 using card/material fixtures, upgraded both through normal development commands and restored their level-10 energy after reload. Their Spine data and idle animations loaded successfully. The world view and mobile roster captures were inspected; the full reference smoke finished without console errors or failed requests.
- The Zhushen package contains 104 owned files. Current host type checking reports zero module errors and 17 other host diagnostics. A runtime test inside the host remains outstanding; no reference assets or converted source tables are exported.

## Validation On 2026-09-06

- 205 tests passed, including area entry/exit, independent lifetimes, following/channel movement, aim locking, stack damage, phased group healing, mode-specific target limits, projectile-created areas and cleanup, plus compound costs, defensive states, projectiles, persistence and host-adapter checks.
- Creator Web Mobile build, full reference browser smoke and independent fixture smoke passed. The reference browser deployed source heroes 2, 26 and 10 with fixture cards/materials; their Spine, level-10 energy and individual modifiers survived reload. Desktop and mobile captures were inspected.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-traits-smoke.mjs` checked source traits and isolated damage contexts. The source tank received 53 damage in PvE versus 66 in PvP. A source health trait changed the base cap from 638 to 669; a temporary cap of 988 saved/restored 334 health. That source character remained in reserve because its battle art is missing. These fixtures do not establish live formula parity.
- The periodic source probe now includes the hero's own 10% periodic bonus: hero 26 produced 54 damage at one layer and 108 damage at two layers/each settlement tick. Hero 2 retained its three 57-damage ticks. The mainline probe still completed nine steps and the first Boss in 201.1 simulated seconds with three survivors.
- Read-only native observation confirmed hero 26's displayed 25% general damage trait and 10% periodic damage trait. No recruitment, upgrade or purchase was performed in the original account.
- The refreshed 104-file Zhushen export passed host type checking with zero module errors and the same 17 other host diagnostics. Actual host runtime validation remains outstanding.
- A second native trait-panel observation confirmed the same 2% normal return on hero 8. The source energy probe separated 200 normal energy and 1000 tactical energy from the configured passive gain, and verified that the normal cast granted no energy before impact. The updated mainline probe completed nine steps in 202.4 simulated seconds with three survivors and 324 vouchers. These are local simulation outcomes, not a measurement of original-game fight duration.
- The same reference map ran inside an isolated copy of the real Zhushen UI managers. Joystick/overview input, back navigation, cached reopen, captured-role saves, and cancellation during configuration/replacement loading passed. Runtime-start messages came through actual host observers. The 3,558 copied source hashes matched their originals; no original working-copy files were installed or changed. See `zhushen-integration.md` for the reproducible probe and its production/network boundaries.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-healing-smoke.mjs` upgraded source hero 16 to level 10 with fixture cards/materials. At 99 attack, the ordinary heal restored 69 health to one ally; the extra and enhanced heals restored 138 to three allies. The ultimate cleansed one eligible NPC effect from each of five allies while retaining a supplied player-origin healing reduction. Expiry restored single-target healing. The ordinary party AI also selected the tactical, extra and normal skills. This is a controlled local configuration test.
- The updated browser probe deployed source hero 16, restored its skill ownership and level after reload, and verified its Spine and portraits. Portrait captures now wait for asynchronous loading to finish. Both standalone and isolated-host regressions passed with the updated combat code.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-impact-smoke.mjs` validates source hero 10 at level 10 with fixture cards/materials and stationary targets. At 85 attack, the tactical dealt 102 damage to four targets and displaced each by 100 world units. The ultimate dealt 102 to six targets, displaced each by 20 and restored 51 health per target to its caster; a wounded ally stayed unchanged. The ordinary party AI also produced a knockback event. This is a local simulation test, not a live timing measurement.
- Native observation of hero 10's ultimate panel confirmed six targets, 120% soul damage and 50% damage-based recovery. The original account's hero levels and lineup were not changed, and its exploration screen was restored afterward.
- The browser impact fixture displayed a cached enemy in its hurt state partway through displacement. With that fixture's existing rank/equipment bonuses it dealt 132 damage and restored 66 health to hero 10 alone. The impact capture and mobile roster were inspected. Both Creator builds, the complete standalone reference/independent smoke runs and the isolated-host regression passed; no console or asset-request failures were recorded. The original host's 3,558 source hashes were unchanged.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-controls-smoke.mjs` checks actual cached Buff rows and the staged source airborne effect against fixture actors. Source freeze expired at 0.75 seconds. The two-second airborne state retained 0.95 seconds after its one-second Buff had expired and remained eligible for NPC cleansing. The source immunity Buff retained all four state markers and prevented incoming freeze and displacement. These are local configuration/runtime results, not measurements of original combat.
- The browser freeze fixture held an existing hero's Spine track time unchanged during a 250-ms wall-clock observation. After the one-second simulated control expired, movement and animation resumed. Full reference and independent browser regressions, both Creator builds and the isolated-host regression passed with the control changes. Desktop/mobile captures were inspected; host type checking had zero module errors and the same 17 other diagnostics, and all 3,558 original host source hashes matched.
- The source control probe also checks the source fear Buff: the current 350-unit/second and 500-ms interval mapping produced three 175-unit segments over 1.5 seconds, then restored voluntary movement. The airborne probe retained more than 150 units of visual elevation after the one-second owner Buff expired. These are controlled local results; the motion parameters still require original-game measurement.
- Browser motion probes confirmed 160 units of control elevation with zero ground displacement and a matching 131.2-unit reference visual offset. The fear fixture moved 87.5 units during a quarter-second sample, rendered the movement animation and stayed on walkable ground while voluntary movement remained blocked. Mobile and desktop airborne/fear captures were inspected. Both Creator builds, full standalone reference/independent regressions and the isolated-host regression passed. Host source hashes remained unchanged and module type checking reported zero module errors.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-projectiles-smoke.mjs` exercises actual source skills on stationary fixture actors. Enemy AI released one source tornado; two targets each received two 200-damage contacts one second apart, while an off-path target remained untouched. The source soul projectile damaged exactly three targets for 216 each and left the fourth unchanged. Both shots expired or exhausted their budget. These probes validate the local source-to-runtime path, not original-game damage or hitbox parity.
- The browser projectile probe moved the source atlas by 50 world units while advancing from frame 2 to frame 5, then verified upward flight with a 90-degree visual heading and complete node cleanup. Mobile, desktop and upward-flight screenshots were inspected. Both Creator builds, full standalone regressions and the final isolated-host regression passed. Module type checking reported zero module errors and the same 17 other diagnostics; all 3,558 original host source hashes remained unchanged.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-defense-smoke.mjs` validates actual cached primitive defense states and the staged armor Buffs on fixture actors. Invulnerability preserved 1,000 HP and a 50-point shield; death protection held one HP until explicit removal; untargetability and healing prohibition reverted on removal. The armor states limited a 500-damage input to one damage for six/three seconds, then a 100-damage input removed 100 HP. The six-second state ended while its 60-second Buff remained. These results do not validate the enclosing skills or their unimplemented conditional modifiers.
- Browser validation confirmed unchanged HP under invulnerability, visible immunity feedback above the reference actor and one damage under the actual staged armor state. Mobile and desktop captures were inspected. Both Creator builds, full reference and independent browser regressions, and the final isolated-host regression passed. Module type checking had zero module errors and the same 17 other diagnostics, and all 3,558 original host source hashes remained unchanged.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-costs-smoke.mjs` validates actual source skills on fixture actors. The compound ultimate refused to cast at zero energy, then consumed 200 of 1,000 HP and 10,000 energy. The source charge-fill skill enabled exactly one charged attack. The enhanced soul skill consumed three supplied charges; the zero-star source base does not produce those charges. A seeded Boss gain produced two charges and allowed exactly two paid follow-ups. These are local resource-contract results, not complete skill parity.
- Browser validation confirmed no HP loss on a failed zero-energy cast, then a 495-HP expenditure with energy reduced to zero under that fixture's grown stats. The source charge skill filled one charge and the follow-up consumed it. Mobile/desktop captures were inspected. Both Creator builds, full standalone reference/independent regressions and the isolated-host regression passed; module type checking had zero module errors and the same 17 other diagnostics, and all 3,558 original host source hashes matched.
- `node --no-warnings --loader ./tests/ts-loader.mjs tools/reference-areas-smoke.mjs` checks actual source area definitions on fixture actors. Poison produced 50/100/150/200/200 damage as its marker stacked, missed the target that left after aim lock, and stopped new damage after exit. The following area matched its caster's 100-unit movement. The source healing field produced exactly 25 heals across five allies and five ticks, totaling 1,500 health at 100 attack. These are local configuration/runtime results and do not prove the unported area modifiers or complete Boss behavior.
- Browser validation displayed the cached poison area, advanced its atlas from frame 1 to frame 4, verified damage stopped after the target left and confirmed area-node cleanup at expiry. Mobile and desktop captures were inspected. Both Creator builds, full standalone reference/independent regressions and the isolated-host regression passed. Module type checking had zero module errors and the same 17 other diagnostics; all 3,558 original host source hashes matched.
